# CLAUDE.md

このファイルは、このリポジトリで Claude Code (claude.ai/code) が作業する際のガイダンスです。

## プロジェクト概要

Python を用いた自動化スクリプトを管理するプロジェクトです。

## Git 運用ルール

- **コードを変更したら、その都度 GitHub にプッシュすること。**
  - 変更をコミットせずに放置しない。1つのまとまった変更（機能追加・修正・設定変更など）が完了するたびに、コミットしてリモート（`origin`）へプッシュする。
  - 作業を中断する場合や、ユーザーへの応答を終える前には、未プッシュの変更が残っていないか確認する。
- コミットメッセージは、変更内容が one line で分かるように簡潔に記述する（日本語・英語どちらでも可）。
- 直接 `main` ブランチに変更を積み重ねる場合も、変更ごとに `git add` → `git commit` → `git push` を徹底する。機能単位で分けたい場合はブランチを切ってよいが、その場合もこまめにプッシュする。
- 破壊的な操作（`git reset --hard`、`git push --force` など）は、明示的な指示がない限り行わない。
- Push 前に `git status` / `git diff` で差分を確認し、意図しないファイル（認証情報、`.env`、一時ファイルなど）が含まれていないことを確認する。

## リポジトリ情報

- リモートリポジトリ: https://github.com/hiro0112/python-automation.git

## 開発時の注意事項

- Python の依存パッケージを追加した場合は `requirements.txt`（または `pyproject.toml`）を更新すること。
- 新しいスクリプトを追加した際は、簡単な説明を README や当該スクリプトのdocstringに残すこと。
- 秘密情報（API キー、パスワードなど）はコードや Git 履歴に含めない。環境変数や `.env`（`.gitignore` 対象）を利用する。
