# python-automation

Python を用いた自動化スクリプト集。

## セットアップ

```bash
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
```

## 運用ルール

このリポジトリでの開発ルール（Git 運用ルールなど）は [CLAUDE.md](./CLAUDE.md) を参照してください。
